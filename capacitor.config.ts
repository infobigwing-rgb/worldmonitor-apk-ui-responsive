import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.worldmonitor.apkui',
  appName: 'WorldMonitor APK UI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
